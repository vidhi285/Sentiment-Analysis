# requirements.txt
# Install all dependencies with:
#   pip install -r requirements.txt

# ─── Existing dependencies (emoji model, MongoDB, YouTube, etc.) ───
pymongo
pandas
scikit-learn
emoji
torch
torchvision
Pillow
matplotlib
google-api-python-client
numpy

# ─── New dependencies for SBERT + Deep Learning text model ─────────
sentence-transformers       # SBERT embeddings (all-MiniLM-L6-v2)
tensorflow                  # Keras deep learning classifier
transformers                # Required by sentence-transformers (Hugging Face)